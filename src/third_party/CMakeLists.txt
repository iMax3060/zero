MESSAGE(STATUS "========================================================")
MESSAGE(STATUS "Start adding Backward-cpp ...")
MESSAGE(STATUS "========================================================")
SET(BACKWARD_SHARED ON)
ADD_SUBDIRECTORY(backward-cpp)
ADD_BACKWARD(zapps)
MESSAGE(STATUS "========================================================")
MESSAGE(STATUS "Finished adding Backward-cpp ...")
MESSAGE(STATUS "========================================================")
MESSAGE(STATUS "========================================================")
MESSAGE(STATUS "Start adding Google Test ...")
MESSAGE(STATUS "========================================================")
ADD_SUBDIRECTORY(googletest)
MESSAGE(STATUS "========================================================")
MESSAGE(STATUS "Finished adding Google Test ...")
MESSAGE(STATUS "========================================================")
MESSAGE(STATUS "========================================================")
MESSAGE(STATUS "Start adding Junction ...")
MESSAGE(STATUS "========================================================")
ADD_SUBDIRECTORY(junction)
MESSAGE(STATUS "========================================================")
MESSAGE(STATUS "Finished adding Junction ...")
MESSAGE(STATUS "========================================================")
